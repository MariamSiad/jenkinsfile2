@Library('sysadmin') _
pipeline {
    agent {
        label 'agent1'
    }
    tools{
        maven 'maven-352'
    } 
    stages {
        stage('Run Shared Library') {
            steps {
                buildJava()
            }
        }
    }
}